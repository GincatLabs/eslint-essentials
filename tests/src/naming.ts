/* eslint-disable max-classes-per-file */
/* eslint-disable no-unused-vars */

// We can use camelCase, PascalCase, UPPER_CASE for variable names.
const myVariable = true;
const MyVariable = true;
const MY_VARIABLE = true;
const my_variable = true; // ❌

// We can use camelCase and PascalCase for function names.
const myFunction = (): boolean => true;
const MyFunction = (): boolean => true;
const MY_FUNCTION = (): boolean => true; // ❌

// Class names should be in PascalCase.
class MyClass {}
class myClass {} // ❌
